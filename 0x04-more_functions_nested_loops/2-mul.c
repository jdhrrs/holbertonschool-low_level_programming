#include "holberton.h"

/**
 * mul - Prints the multplication of two integers.
 * @a: First integer
 * @b: Second integer
 *
 * Return: Returns the result (int).
 */

int mul(int a, int b)
{
	return (a * b);
}
